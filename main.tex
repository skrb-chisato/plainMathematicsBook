% Template name: Plain mathematics book
% Version: 0.1.1
% Author: Chisato

% Do not change the command order or remove spaces to avoid errors.

% Environment

\documentclass[oneside]{book} % Specify the document class as "book" with a "oneside" option for a single-sided layout.

\usepackage{plainBook} % Import the "plainBook" template style package.

% Metadata

\newcommand{\bookTitle}{Lorem ipsum} % Set the title of book.
\newcommand{\bookSubtitle}{Neque porro quisquam} % Set the subtitle of book.
\newcommand{\bookAuthor}{Chisato} % Set the authors of book.

% Document body

\begin{document}
    \begin{spacing}{1.5} % Set line spacing to 1.5 for the entire document.
        
        % Front matter
        
        \frontmatter
        
        \title{\Huge{\textbf{\bookTitle}} \\ \LARGE{\bookSubtitle}} % Define the title with specified formatting.
        \author{\bookAuthor} % Define the authors with specified formatting.
        \date{} % Date is left empty.

        \maketitle % Generate and display the title, author, and date.

        \include{content/frontmatter/preface.tex} % Include the preface.
        
        \setcounter{tocdepth}{1} % Set the depth level for the table of contents (1 includes sections only).
        \tableofcontents % Generate and displays the table of contents.

        % Main matter

        \mainmatter
        
        \part{Maecenas}
            \include{content/mainmatter/1Part/1Chapter.tex}

        \appendix
            \include{content/mainmatter/appendix/1appendix.tex}
        
        % Back matter
        
        \backmatter
        
        \include{content/backmatter/index.tex} % Include the index.
        \include{content/backmatter/bibliography.tex} % Include the bibliography.
    \end{spacing}
\end{document}
